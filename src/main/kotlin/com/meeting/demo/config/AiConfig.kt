package com.meeting.demo.config

import org.springframework.context.annotation.Configuration

@Configuration
class AiConfig {
    // Spring AI OpenAI beans are auto-configured
    // You can inject ChatClient, ChatModel, or EmbeddingModel directly
}